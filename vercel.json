{
  "version": 2,
  "buildCommand": "cd frontend && npm install --legacy-peer-deps --loglevel=error && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm install --legacy-peer-deps --loglevel=error",
  "github": {
    "silent": false
  }
}
